(function(){
'use strict';
const key='basyal-student-case-hash-v1';
let seed=localStorage.getItem(key)||'';
const plans={
 neural:[['Retail churn','Predict whether a loyalty customer will stop purchasing from signals such as visit frequency and discount use.','Compare 2, 4, and 8 hidden units; then test two learning rates and added noise.'],['Campus event response','Predict whether a student will attend an event from interest and schedule-fit signals.','Start with XOR, then compare capacity and noise sensitivity.'],['Donation follow-up','Classify which past donors are likely to respond to an outreach campaign.','Compare activation, hidden units, and learning rate while holding data constant.']],
 vision:[['Package inspection','A fulfillment team wants to distinguish vertical and horizontal package-label marks from tiny grayscale images.','Compare edge filters, edit pixels, and test at least two noise levels before recommending a confidence threshold.'],['Shelf monitoring','A retailer wants a simple vision cue for horizontal shelf gaps versus vertical dividers.','Build both patterns, compare convolution kernels, and identify a noisy case that makes the classifier ambiguous.'],['Document routing','A mailroom uses line orientation as one weak feature in a document-routing prototype.','Compare filters on vertical, horizontal, and cross patterns; explain why the feature alone is insufficient.']],
 ml:[['Customer retention','A service team must classify a new customer as likely to stay or leave from satisfaction and usage coordinates.','Place the query near a boundary; compare k=1, 5, and 11 with Euclidean and Manhattan distance.'],['Loan follow-up','A community lender prioritizes applicants for manual follow-up using two normalized risk indicators.','Test small and large k values, then explain the vote margin and the effect of scaling.'],['Support triage','Classify a new support case as routine or urgent from impact and complexity coordinates.','Move one query point across the boundary and compare distance metrics without changing k.']],
 xai:[['Microloan review','Explain a transparent eligibility score without claiming that a feature caused the decision.','Change one feature at a time, then move the threshold while restoring the original features.'],['Student support','Explain a transparent outreach-priority score based on academic and preparation signals.','Identify the largest contribution and one score change that does not change the decision.'],['Hiring screen','Audit a toy screening score and distinguish local explanation from fairness evidence.','Compare two feature interventions and one threshold intervention.']],
 sent:[['Product reviews','A small retailer needs to flag clearly negative feedback while preserving mixed reviews.','Write positive, negative, mixed, negated, sarcastic, and domain-specific examples; compare both modes.'],['Campus services','Analyze short comments about advising, dining, or registration.','Build six original sentences and document the exact token contributions for one failure.'],['Brand monitoring','Test whether a simple rule system can interpret campaign reactions.','Vary wording and threshold, then explain why sarcasm defeats keyword counting.']],
 temperature:[['Executive-summary wording','A communication team wants consistent factual wording without making every draft identical.','Compare temperature, top-k, and top-p while holding logits constant; recommend a sampling policy and its limitation.'],['Product ideation','A product team wants diverse idea verbs while keeping the prompt direction relevant.','Compare low- and high-entropy settings, then change logits and explain how model scores differ from sampling controls.'],['Customer-service phrasing','A support team needs controlled variation in response openings.','Run at least six sampling configurations and identify a setting that balances concentration and diversity.']],
 agentic:[['Inventory exception','Design a transparent agent workflow that investigates low stock without sending an unapproved supplier message.','Vary tools, step budgets, and approval policies; explain which safeguard prevents the riskiest action.'],['Campaign review','Configure an agent to compare marketing performance while keeping calculations and public benchmarks traceable.','Remove one tool, change the budget, and compare the resulting traces and verification gaps.'],['Travel policy','Explore an agent that drafts a policy recommendation but keeps human authority over communication.','Compare approval policies and identify the minimum evidence required before any external action.']],
 gradient:[['Advertising allocation','Minimize a toy campaign error function to choose a budget parameter.','Compare learning rates 0.04, 0.12, and 0.8, with and without momentum.'],['Inventory forecast','Minimize forecasting loss from two start points.','Use the convex surface first, then show how the same settings behave on the wavy surface.'],['Price tuning','Find a low-loss price parameter while avoiding unstable updates.','Hold the start point constant while changing learning rate and momentum.']],
 clustering:[['Customer segments','Group customers from normalized frequency and value coordinates without labels.','Compare k=2, 3, and 4; report inertia but also judge whether each grouping is useful.'],['Donation patterns','Explore donor groups from recency and contribution coordinates.','Compare spread and random initialization, then test the rings dataset as a failure case.'],['Store profiles','Cluster locations using normalized traffic and basket-size measures.','Test three k values and explain why lower inertia alone cannot select the business meaning.']]
};
function hash(text){let h=2166136261;for(const ch of text.trim().toLowerCase()){h^=ch.charCodeAt(0);h=Math.imul(h,16777619);}return h>>>0;}
function pick(lab){if(!seed||!plans[lab])return null;const list=plans[lab],n=hash(seed+':'+lab),p=list[n%list.length];return{code:`${lab.toUpperCase()}-${n.toString(16).slice(-6).toUpperCase()}`,context:p[0],problem:p[1],method:p[2],variant:2+(n%7)};}
function html(lab){const c=pick(lab);if(!c)return'';return `<div class="assigned-case"><div class="assigned-case-head"><div><span>Your assigned sample problem</span><strong>${c.context}</strong></div><b>${c.code}</b></div><p>${c.problem}</p><h4>How to use this lab</h4><p>${c.method} Complete at least <strong>${c.variant}</strong> controlled comparisons and explain which hyperparameter or option changed the result, why it changed, and one limitation.</p><small>Keep this case code in your report. The same student ID always generates the same case; the original ID is not retained.</small></div>`;}
function init(){const form=document.getElementById('caseForm'),input=document.getElementById('caseStudentId'),status=document.getElementById('caseStatus');if(!form)return;if(seed)status.textContent=`Cases ready · private code ${seed.slice(-8).toUpperCase()}`;form.addEventListener('submit',e=>{e.preventDefault();const raw=input.value.trim();if(!raw)return;seed=hash(raw).toString(16).padStart(8,'0');localStorage.setItem(key,seed);input.value='';status.textContent=`Cases ready · private code ${seed.toUpperCase()} · original ID discarded`;document.dispatchEvent(new CustomEvent('basyal-case-ready'));document.getElementById('graderStudent').value='';document.querySelector('[data-open-lab="neural"]')?.focus();});}
window.StudentCases={html,pick};
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();
})();
